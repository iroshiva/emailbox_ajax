

<% if @email.read == true %>
	$('.lu').text("Marquer comme non lu");
	$('.list-email-<%= @email.id%>').removeClass("list-group-item-dark");
<% else %> 
	$('.lu').text("Marquer comme lu");
	$('.list-email-<%= @email.id%>').addClass("list-group-item-dark");
<% end %>

